# Xschem steps

1. Run `xschem`.
2. Create `comparator.sch`.
3. Add differential input pair, active load, gain stage, output inverter/buffer.
4. Add PDK symbols after selecting PDK with `eda-env` or `sak-pdk`.
5. Generate SPICE netlist.
6. Run ngspice.
